*.zip
.DS_Store
node_modules/

# signing keys / packaged CRX — never commit
*.pem
*.crx

# design source exports (kept locally, not committed)
store-listing/*.zip

# firebase debug log (local junk)
firebase-debug.log
